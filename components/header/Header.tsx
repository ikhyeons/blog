import React from 'react';
import styles from '@/styles/components/layout/Header.module.scss';

function Header() {
  return <header className={styles.header}>어서오세요!</header>;
}

export default Header;
